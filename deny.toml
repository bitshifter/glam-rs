[bans]
multiple-versions = "deny"
deny = []
skip-tree = [
    # ignore criterion dev-dependency that often have duplicate dependencies internally
    { name = "criterion" },
    # encase 0.12 is intentionally supported alongside 0.13, see #857
    { crate = "encase:>=0.12,<0.13", reason = "encase 0.12 is supported alongside 0.13" },
]

[licenses]
allow = [
    "Apache-2.0",
    "MIT",
    "MIT-0",
    "Unicode-3.0",
    "Zlib",
]

[advisories]
# ignoring bincode unmaintained advisory as it's a dependency of a dev-dependency it's low risk to users.
ignore = [
    "RUSTSEC-2025-0141"
]
